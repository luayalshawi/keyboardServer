# keyboardServer
Server implementation based on nodejs using express.js

# Step 1
Clone prject

# Step 2
From CL "npm install" to dowload dependencies.

# Step 3
From CL "node ./index.js"

# Step 4
Server will be running on port 5000.
